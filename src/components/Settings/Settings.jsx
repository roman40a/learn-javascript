import React from 'react';
import stl from './Settings.module.css';

const Settings = (probs) => {
    return (
        <div className={stl.settings}>
            Settings
        </div>
    );
}

export default Settings;